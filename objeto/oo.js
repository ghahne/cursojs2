// CÓDIGO NÃO EXECUTÁVEL

// Procedural (exemplo de linguagem: C)
processamento(valor1, valor2, valor3)

// OO
objeto = {
    valor1,
    valor2,
    valor3,
    processamento() {
        // ...
    }
}

// acessariámos a função processamento acima da seguinte forma
objeto.processamento()

// Principios importantes
// .1 abstração
// .2 encapsulamento (esconder detalhes de implementação, não deixar exposto os detalhes do objeto para não existir alto nível de ACOPLAMENTO e não gerar dependência)
// .3 herança (prototype)
// .4 polimorfismo (várias formas, esse conceito faz mais sentido em linguagens tipadas)
